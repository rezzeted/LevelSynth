# Parity scenarios (stub, iteration 0)

Placeholder for scenario definitions (seed, level snippet, expected artifact path). No pipeline consumes this folder yet.

When adding the first golden: create a subfolder or `scenario_*.md` / `*.json` here and reference it from [../README.md](../README.md).
